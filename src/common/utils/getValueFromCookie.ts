"use client";
export const getCookie = (key: string) => {
  // const value = window.document.cookie.match(
  //   "(^|;)\\s*" + key + "\\s*=\\s*([^;]+)"
  // );

  // return value ? value.pop() : "";
  return null
};
